# GENERATED by scripts/make_curve_scenarios.py — edit that, not this.
#
# One point on the learning curve. Everything except the training
# manifest, freeze_backbone and the learning rate is identical across
# every point, so a difference between two points can only be the data
# size or the regime.
card:
  group: Learning curve — how much data is enough?
  lineage: ResNet18 · linear probe
  name: ResNet18 linear probe, n=7,014
  emoji: 📈
  description: Trained on 7,014 images drawn from isic_train.csv by lesion, linear
    probe, and evaluated on the same 1,493 images as every other run. One point on
    a curve whose question is where more data stops helping.
dataset:
  id: ham10000-clean-test
  images_dir: data/raw/ham10000
  loader: verifai.datasets.loaders:load_image_manifest
  manifest: data/manifests/ham10000_test.csv
  source: ISIC 2019 (training) / HAM10000 (evaluation)
domain: image
integrity:
  enforce: true
  group_key: lesion_id
metrics:
- integrity.split_leakage
- performance.classification
model:
  arch: resnet18
  cam_layer: layer4[-1]
  classes:
  - actinic_keratoses
  - basal_cell_carcinoma
  - benign_keratosis-like_lesions
  - dermatofibroma
  - melanocytic_Nevi
  - melanoma
  - vascular_lesions
  device: auto
  id: resnet18-probe-n7014
  loader: verifai.models.image:load
  weights_path: artifacts_training/curve_probe_n7014.pt
name: curve_probe_n7014
label: "linear probe, n=7,014"
sample_size: null
seed: 42
training:
  arch: resnet18
  batch_size: 32
  class_weights: true
  device: auto
  epochs: 12
  image_size: 224
  images_dir: data/raw/isic2019
  lr: 0.001
  manifest_dir: data/manifests
  manifest_prefix: isic_n7014
  out_dir: artifacts_training
  pretrained: true
  workers: 8
  freeze_backbone: true
